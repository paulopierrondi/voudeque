# Cursor Remote Limitations — voudeque

Registry generated at: `2026-05-21T12:27:30Z`

No `.cursor/environment.json` was generated automatically.

Reason: `unsupported`.

Cursor Background Agent may still edit files in a branch, but local validation may require Paulo's Mac, Codex, Claude Code, Gemini CLI or Google Antigravity.

Common local-only areas:

- Xcode build/signing.
- Apple Keychain.
- App Store Connect/TestFlight upload.
- Provider sessions and secrets.
- Local Obsidian vault reads outside committed `.brain` snapshots.

When blocked, the agent must leave a handoff summary instead of guessing.
